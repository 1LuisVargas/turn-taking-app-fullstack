const NavBar = () => {
    return (
        <nav>
            <a href="#">Home</a>
            <a href="#">Appointments</a>
            <a href="#">Login</a>
        </nav>
    );
};

export default NavBar;